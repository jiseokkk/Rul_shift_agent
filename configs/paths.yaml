# 외부 데이터 경로. 코드에서 경로를 하드코딩하지 않고 여기만 본다 (CLAUDE.md 규칙 8).
# datasets/ 아래는 읽기 전용으로만 취급한다 (규칙 1).

# N-CMAPSS 원본 HDF5 — clean train/calibration reference 구축용 (dev split)
ncmapss_h5: /home/iai4/Desktop/han/dataset/data_set/N-CMAPSS_DS02-006.h5

# 오염 주입된 시나리오 루트 (series.npz + spec.json)
corrupted_root: /home/iai4/Desktop/han/dataset/corrupted_dataset

# Ground Truth. evaluation/ 에서만 읽는다 (규칙 2).
manifest_csv: /home/iai4/Desktop/han/dataset/corrupted_dataset/manifest.csv

# 다른 머신에서 마운트 경로가 다르면: AGENT_RUL_PATH_MAP="/home/iai4=X:/home/iai4"

# frozen RUL 모델 (프로젝트 안에 복사해 둔 weight)
frozen_rul_dir: models/frozen_rul     # 프로젝트 루트 기준 상대경로

# 산출물 루트 (프로젝트 루트 기준 상대경로)
artifacts_root: artifacts
results_root: results
