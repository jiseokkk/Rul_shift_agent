# 모든 경로는 프로젝트 루트(agent_RUL/) 기준 상대 경로.
# 심볼릭 링크 대신 여기서 원본 위치를 지정한다 (OS 무관).

rul_repo: RUL/two_phase             # 원본 clone (mjliu-advertising/RUL, 수정 금지). 다른 모델을 붙일 때는 RUL/<name>/ 에 나란히 둔다
raw_dir: RUL/two_phase/dataset/CMAPSS   # train/test/RUL_FD00X.txt

data_dir: data
split_dir: data/split
norm_dir: data/norm
shifted_dir: data/shifted

models_dir: models
preds_dir: preds
labels_dir: labels
reports_dir: reports
