"""평가 — **GT(manifest.csv, true RUL, t_f, fault 센서)를 읽는 유일한 패키지** (CLAUDE.md 규칙 2).

gt · classify(event + 12.4 규칙) · metrics(13 지표 + sensitivity) · report(evaluate / report 실행)
tools/, agent/, data.py, reference/ 에서 이 패키지를 import 하면 tests/test_gt_isolation.py 가 잡는다.
"""
